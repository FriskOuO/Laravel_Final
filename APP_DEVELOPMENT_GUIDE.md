# Mood Diary 專案開發更新與 App 對接指南 (2026-06-07)

這份文件彙整了目前 Web 端的重大更新，旨在幫助 App 開發團隊同步最新的設計語言、後端邏輯以及使用者體驗優化。

---

## 1. 核心設計語言 (Artistic Design System)
Web 端已全面升級為「沉浸式藝術風格」，App 端建議參考以下視覺規範：

### A. 配色系統 (Theming)
*   **日間模式 (Warm White)**：
    *   主背景：`#fdfaf6` (暖米色)
    *   主品牌色：`#b45309` (Amber-700)
    *   強調色（Subtle）：`#fffaf0` / `#ffedd5`
*   **黑夜模式 (Midnight Blue)**：
    *   主背景：`#020617` (深藍黑)
    *   主品牌色：`#38bdf8` (Sky-400)
*   **視覺元素**：
    *   **大圓角**：卡片與彈窗均使用 `40px` ~ `48px` 的圓角。
    *   **柔和陰影**：避免死黑，使用極低不透明度的主題色陰影。
    *   **背景裝飾**：使用大型、高模糊度（Blur 120px）的背景光暈 (Blobs) 增加層次感。

### B. 環境感知背景
*   **日間**：背景包含緩慢漂移的雲朵（CSS Drift Animation）與右上角的琥珀色太陽。
*   **黑夜**：背景包含呼吸感閃爍的星空（Twinkle effect）與清冷月亮。

---

## 2. 後端功能與 API 異動
App 開發者需注意以下資料結構與權限的更動：

### A. 公開/私人隱私設定
*   **資料表異動**：`posts` 表新增了 `is_public` (boolean) 欄位。
*   **發佈邏輯**：
    *   預設為 `Private` (false)。
    *   當設定為 `Public` 時，前端需先顯示**免責聲明彈窗**，確認內容符合規範。
*   **權限隔離 (重要)**：
    *   `GET /api/diaries`：現在支援訪客存取。
    *   **訪客**：僅回傳 `is_public: true` 的全站日記。
    *   **登入使用者**：回傳「自己所有的日記」＋「全站其他人的公開日記」。
    *   **管理員**：回傳全站所有日記。

### B. 關聯資料預載 (Eager Loading)
*   所有日記 API 現已加入 `with('user')`。
*   **卡片顯示**：若 `user_id` 與當前登入者不同，必須顯示 `Author: [Name]` 標籤。

---

## 3. 使用者路徑與 UX 優化
App 應遵循以下路徑邏輯以確保一致性：

*   **登入導向**：
    *   訪客：預設進入介紹首頁（Home）。
    *   管理員：登入後直接導向「系統管理主控台 (Admin Dashboard)」。
*   **資料過濾**：
    *   「日記」列表：顯示「自己 + 公開內容」。
    *   「歷史回顧」：**嚴格僅顯示該使用者本人**的文章。
    *   「管理員控制台」：應包含「心情比例分佈圖」（建議使用圓餅圖/甜甜圈圖）。
*   **載入體驗**：
    *   全站各視圖切換應配備 **骨架屏 (Skeleton Screens)** 閃爍動畫，減少等待焦慮。
*   **刪除操作**：
    *   刪除資料時應觸發「向右滑動淡出」的動畫效果。

---

## 4. 關鍵組件參考
*   **自定義切換開關 (Privacy Toggle)**：加大尺寸，開啟時具備發光效果與狀態文字提示。
*   **藝術感彈窗 (Artistic Modal)**：使用強大的高斯模糊背景 (`backdrop-filter`) 與漂浮動畫圖示，全面取代系統原生 Alert/Confirm。

---

**備註**：詳細的 API 格式請參閱 `PostApiController.php` 與 `StorePostRequest.php`。
